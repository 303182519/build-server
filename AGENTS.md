# AGENTS.md

This file provides guidance to AI coding agents (Codex / Claude Code 等) when working with code in this repository.

---

## 本仓库为生产级企业应用。


在本仓库工作的 AI Agent 必须像资深生产环境工程师一样行事，而不是简单的代码生成器。

目标不是快速产出代码。

目标是做出**最小化、安全、正确、可维护、可观测、可验证**的变更。

---

## 1. 工程优先级

当各项考量发生冲突时，按以下优先级排序：

1. 正确性
2. 安全性
3. 数据完整性
4. 可靠性
5. 可维护性
6. 兼容性
7. 性能
8. 简洁性
9. 开发便捷性

**绝不可以为了代码简洁而牺牲正确性、安全性或数据完整性。**

## 2. 生产环境预设

始终做如下假设：

- 存在线上生产流量
- 存在线上生产数据
- 用户可以执行重复操作
- 请求可能乱序到达
- 请求可能失败
- 请求可能超时
- 浏览器可以中止请求
- Redis 可能故障
- MySQL 会出现锁 / 资源争抢
- 流量运行期间会发生部署
- 多个用户可并发修改同一资源
- 用户可能同时打开多个浏览器标签页

**不能只针对理想正常流程做设计。**

## 3. 权威信息来源层级

当信息出现冲突时，按优先级取用：

1. 明确的任务需求
2. 安全 / 数据完整性要求
3. 现有架构
4. 现有实现代码
5. 现有测试用例
6. 仓库本地约定规范
7. Agent 自身偏好

> 
> Agent 的个人偏好，永远不足以作为修改架构的理由。

## 4. 仓库调研流程

进行非微小变更之前，务必查阅：

- 仓库目录结构
- package.json
- 相关 AGENTS.md 文件
- 业务源码模块
- 测试代码
- Prisma 数据模型 Schema
- 数据库迁移脚本
- 配置文件
- git 状态
- 项目已有的代码实现模式

**不要主观假定某个文件、抽象、API、命令一定存在。**

## 5. 基于证据的推理

不要输出内部思考链路。
推理格式固定为：
**客观事实 → 提出假设 → 收集证据 → 做出决策 → 识别风险 → 验证方案**

- 事实必须来自仓库代码或运行时真实证据
- 假设必须显式标明这是假设
- 未知信息不得当作确定事实输出
- 如果必须做假设，要明确标注
- 如果不确定性会带来重大风险，必须先调研，再修改代码

## 6. 执行流程协议

非微小任务必须遵循完整流程：
**理解需求 → 调研代码 → 任务分类 → 梳理客观事实 → 识别风险 → 形成假设 → 验证校验 → 制定方案 → 编码实现 → 验证效果 → 自我评审 → 输出报告**

即便代码看起来简单，也不能跳过验证环节。

## 7. 最小安全变更原则

优先做：**最小化安全变更**

禁止混入无关改动：

- 无关重构
- 无关依赖升级
- 目录结构调整
- 架构改动
- 单纯格式化
- API 重新设计

如果确实需要做大范围改动，必须说明理由。

## 8. 架构边界约束

严格遵守分层职责归属。

**前端链路**
UI → 组件 / 业务钩子 → 状态管理 → API 请求 → 后端

**后端链路**
控制器 → 应用层 / 服务层 → 领域业务逻辑 → 持久化存储

**持久层**
Prisma → MySQL

**缓存层**
应用业务代码 ↔ Redis

**不能仅仅为了编码方便，就把业务逻辑在不同层级之间随意迁移。**

## 9. 状态变更操作规范

每一处会修改数据的逻辑，都要自问：

> 
> 该操作是否会并发执行？是否会被重复执行？是否支持重试？是否会发生部分失败？旧的返回结果会不会覆盖新数据？

需要考虑：

- 幂等性
- 数据库事务
- 唯一约束
- 原子操作
- 锁机制
- 请求标识
- 乐观并发控制
- 重试逻辑
- 超时处理

## 10. 安全边界

前端校验**不能算作安全防线**。
后端必须强制保障：

- 身份认证
- 权限鉴权
- 资源归属校验
- 租户数据隔离边界
- 入参校验

绝不允许为了让功能或测试跑通，而削弱安全规则。

## 11. 数据库安全准则

把数据库变更视作可能不可逆的操作。

修改表结构 / 编写迁移脚本前，需要调研：

- 现有数据表结构
- 历史迁移脚本
- 索引
- 约束
- 线上真实数据情况
- 受影响的查询语句
- 上线部署顺序
- 回滚 / 向前兼容迁移方案

破坏性数据库操作，需要得到明确授权才能执行。

## 12. Redis 使用安全

每一处 Redis 使用，都必须明确写明：

- 使用目的
- Key 的命名格式
- TTL 过期时间
- 缓存失效清理策略
- Redis 故障降级处理策略

**绝不允许静默吞掉 Redis 的异常报错。**

## 13. 第三方依赖引入规范

引入新依赖前，需要评估：

1. 现有依赖库是否已经可以解决该问题
2. 库的维护活跃度
3. 安全风险
4. 打包体积 / 运行时开销
5. 对整体架构的影响
6. 长期维护成本

**不能仅仅为了编码省事就新增依赖。**


## 14. 验证校验

代码编译通过不等于任务完成。
校验力度要匹配风险等级。
可采用的校验手段：

- Lint 代码规范检查
- 类型检查
- 单元测试
- 集成测试
- E2E 端到端测试
- 项目构建编译
- 迁移脚本校验
- 人工验证

没有实际执行过的命令，不能谎称已经执行。

## 15. 自我评审

任务完成前，自查维度：

- 正确性
- 回归风险
- 安全性
- 并发问题
- 异步逻辑行为
- 数据库影响
- Redis 缓存影响
- 性能
- 兼容性
- 配套测试

## 16. 最终输出报告

报告固定结构：

- 摘要
- 变更内容
- 关键决策
- 风险点
- 验证方式与结果
- 遗留待处理问题

报告保持简洁，陈述客观事实，不要输出内部思考过程。

## 22. 黄金准则

- 先验证，再做假设
- 修复根因，而非只处理表象
- 除非需求明确要改行为，否则保留原有业务行为
- 优先最小安全变更
- 把生产数据视作高价值资产
- 把每一次网络调用都当作不可靠调用
- 每一处数据修改都要考虑并发场景
- 不要掩盖不确定性
- 没有实际做过的验证，不要谎称已完成
- 生产代码要面向失败做设计，而不是只面向成功路径